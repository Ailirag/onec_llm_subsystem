
// Интеграционный модуль переопределяет этот метод и сопоставляет только известные
// коды со статическими вызовами. Обработчик работает в транзакции, без привилегий.
// Не выполнять HTTP-вызовы, не управлять транзакцией; UUID операции — ключ идемпотентности.
Процедура ОбработатьРезультат(КодОбработчика, Контракт, Обработан) Экспорт
	Обработан = Ложь;
КонецПроцедуры
